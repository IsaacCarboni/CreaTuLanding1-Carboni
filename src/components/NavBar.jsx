import CartWidget from "./CartWidget";
function NavBar(){
     return (
         <nav>
            <h2>Bodega del Isaa</h2>
            <CartWidget />
            </nav>
            );
            }
            export default NavBar;